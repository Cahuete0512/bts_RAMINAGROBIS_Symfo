{% extends 'base.html.twig' %}

{% block title %}Enchères{% endblock %}

{% block body %}
    <br><div class="app-bar pos-absolute bg-black z-2" >
    <div class="cell-md-4"><img width="100px" height="60px" src="{{ asset('logoCentrale.PNG')}}" style="position:center;top:-10px" alt="#"></div>
    <div class="cell-md-5 pos-fixed pos-top-right">
        <h2 style ="color: #FFFFFF; text-align: left;">{{ fournisseur.societe }}</h2>
    </div>
</div>
    <div class="table table-border cell-border"><br>
        <p><table class="table table-border cell-border">
            <thead>
            <tr>
                <th colspan="2"><strong>Liste des Produits</strong>
                    <label>
                        <input type="text" value="recherche produits" style="height: 30px; width: 200px; float:right;">
                    </label>
                </th>
                <th><strong>Proposition Nouveau Prix</strong></th>
                <th><strong>Votre Position</strong></th>
            </tr>
            </thead>
            <tbody>
            {% for lignePanier in lignesPaniers %}
                <tr>
                    <td>{{ lignePanier.reference }}</td>
                    <td>{{ lignePanier.quantite }}</td>
                    <td>
                        {%  set prix = 0 %}
                        {%  if lignePanier.encheres is not empty %}
                            {%  set prix = lignePanier.encheres[0].prixEnchere %}
                        {% endif %}
                        <div class="enchere">
                            <input id="lignePanier_{{ loop.index0 }}" type="number" value="{{ prix }}">
                            <button class="button success outline small rounded" style="float: right" onclick="">valider</button>
                        </div>
                    </td>
                    <td><div class="cercle_vert">wxcwc</div></td>
                </tr>
            {%  endfor  %}
            </tbody>
        </table>
    </div>
{% endblock %}
